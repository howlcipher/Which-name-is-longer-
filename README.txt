A simple question my daughter asked me over and over again.  So I made a program to give the exact results whenever she wants them.

The program takes two strings(names) and compares the length for which is the longest and displays the total characters.